# Gargoyle build outputs -- generated by build.sh / rebuild.sh *inside* the
# source tree, per build, and can run to tens of GB. Never commit them.
/*-src/
/built/
/compressed_css/
/compressed_javascript/
/minifiers/
/images/
/package-prepare/
/build-*.log
/rebuild-*.log
